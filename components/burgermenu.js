import { isAfter, set } from "date-fns";
import { useEffect, useState } from "react";
import styles from "../styles/modules/burger.module.scss";
export default function Burger({toggle}) {

 
//   if(open){
//       console.log(toggle);
//   }

  return (
    <>
     
    </>
  );
}
